// UI: Status/Trace page (automation step display)

export default function StatusPage() {
  // TODO Phase 5: Implement trace timeline
  // - Poll GET /api/status/:requestId
  // - Display steps with status (running/success/failed)
  // - Show duration for each step

  return (
    <div>
      <h1>Status/Trace Page</h1>
      <p>TODO Phase 5</p>
    </div>
  );
}
