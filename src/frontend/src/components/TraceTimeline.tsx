// Component: Trace timeline display

import type { TraceStep } from '../../../shared/types/Trace';

interface TraceTimelineProps {
  steps: TraceStep[];
}

export default function TraceTimeline({ steps }: TraceTimelineProps) {
  // TODO Phase 5: Implement trace timeline UI
  // - Display each step with status icon
  // - Show duration
  // - Highlight errors

  return (
    <div>
      <p>Trace Timeline - TODO Phase 5</p>
    </div>
  );
}
